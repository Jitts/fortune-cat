node_modules/
.next/
/out/
/build
.DS_Store
*.pem
npm-debug.log*
.env*.local
.env
.vercel
*.tsbuildinfo
next-env.d.ts
.env*
dev-server.cmd
supabase/.temp/

# Claude Code local state (transient agent worktrees, personal settings)
.claude/worktrees/
.claude/settings.local.json
.claude/settings.local.json.*

# Impeccable runtime state: a live overlay server's pid/port, written while the
# browser session is open and meaningless once it closes. Critique snapshots
# under .impeccable/critique/ are deliberately NOT ignored — those are a design
# record, and /impeccable polish reads them as its backlog.
.impeccable/live/
